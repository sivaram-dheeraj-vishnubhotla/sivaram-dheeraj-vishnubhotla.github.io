import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://sivaram-dheeraj-vishnubhotla.github.io',
  base: '/',
  output: 'static',
  devToolbar: {
    enabled: false
  }
});
